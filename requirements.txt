Django>=4.2,<7.0
djangorestframework>=3.14
djangorestframework-simplejwt>=5.3
django-cors-headers>=4.3
drf-spectacular>=0.27
Pillow>=10.0
whitenoise>=6.6

# Production deployment (Railway): a real WSGI server, plus Postgres support
# since Railway containers don't keep local files (sqlite3) across deploys.
gunicorn>=21.2
dj-database-url>=2.2
psycopg2-binary>=2.9
